name: Backend CI
on:
  push:
    paths:
      - "be/**"
  pull_request:
    paths:
      - "be/**"
jobs:
  test:
    runs-on: unbuntu-lastest
    defaults:
      run:
        working-directory: be
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: checkout code
        uses: actions/checkout@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: npm tests
        run: npm test
