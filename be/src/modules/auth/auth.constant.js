const MAX_LOGIN_ATTEMPTS = 5;
const LOCK_TIME = 15 * 60 * 1000;
const VERIFICATION_TOKEN_EXPIRES_TIME = 30 * 60 * 1000;
const RESET_PASSWORD_TOKEN_EXPIRES_TIME = 15 * 60 * 1000;
module.exports = {
  MAX_LOGIN_ATTEMPTS,
  LOCK_TIME,
  VERIFICATION_TOKEN_EXPIRES_TIME,
  RESET_PASSWORD_TOKEN_EXPIRES_TIME,
};
